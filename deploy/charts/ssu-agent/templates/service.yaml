apiVersion: v1
kind: Service
metadata:
  name: {{ include "ssu-agent.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ssu-agent.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ssu-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
